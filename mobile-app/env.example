# Expo public env vars (used at build time)
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000
EXPO_PUBLIC_SENTRY_DSN=

# Backend-only env var (used by Next.js mobile auth endpoints)
# MOBILE_JWT_SECRET=change-me

